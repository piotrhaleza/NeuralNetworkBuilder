<Window x:Class="MachineLearningWpfUI.Views.PropertiesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MachineLearningWpfUI.Views"
        mc:Ignorable="d"
        Title="PropertiesWindow" 
        ResizeMode="NoResize" 
        Height="550" Width="400"
      WindowStyle="None"
AllowsTransparency="True"
Background="Transparent"
       >
    <Border  Background="#222222" 
        BorderBrush="#aaaaaa"
        BorderThickness="2" CornerRadius="25" Padding="10" >
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="120"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <TextBlock Foreground="#eeeeee" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="30" Text="f(x) = ax + b"/>
            <Border HorizontalAlignment="Stretch" Margin="15,0,15,0" Height="3" Background="White" Grid.Row="1" CornerRadius="3"/>
            <ToggleButton Width="50" Height="50" Margin="0,10,0,0" Grid.Row="2"/>
        </Grid>
    </Border>

</Window>